# Runtime data containing secrets or personal user data — never commit
data/google-config.json
data/users.json
data/feedback.json

# Atomic-write temp files (created transiently by writeJson)
data/*.tmp.*

# Office lock files
~$*.xlsx
~$*.xls
~$*.docx
